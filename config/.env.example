# Copy to project root as .env or set these in the shell.
# Telegram (user account via Telethon) — get api_id/api_hash from https://my.telegram.org
TELEGRAM_API_ID=26874679
TELEGRAM_API_HASH=bbdaf41297108352425ea2531fea0147
TELEGRAM_PHONE=+201010622257

# Comma-separated channel usernames (without @) to search when using --telegram
TELEGRAM_CHANNELS=muthanapress84

# SQLite database path (default: ./data/investigation.db)
DATABASE_URL=sqlite:///./data/investigation.db
